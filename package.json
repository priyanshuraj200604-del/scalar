{
  "name": "amazon-clone",
  "version": "1.0.0",
  "description": "Full-stack Amazon Clone E-Commerce Platform",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "npm --prefix client run dev",
    "install-all": "npm install && npm --prefix client install",
    "build": "npm --prefix client install && npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
